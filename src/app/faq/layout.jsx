export const metadata = {
  title: "FAQ",
  description:
    "Answers to common questions about gopwnit — labs, CTF competitions, scoring, hosting your own event, and data security.",
};

export default function FaqLayout({ children }) {
  return children;
}
