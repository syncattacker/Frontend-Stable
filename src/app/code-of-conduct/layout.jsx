export const metadata = {
  title: "Code of Conduct",
  description:
    "The standards every player, host, and organizer agrees to on gopwnit — fair play, scope boundaries, and how we handle violations.",
};

export default function CodeOfConductLayout({ children }) {
  return children;
}
