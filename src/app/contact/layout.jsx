export const metadata = {
  title: "Contact",
  description:
    "Get in touch with gopwnit — email us directly or reach the team through Discord.",
};

export default function ContactLayout({ children }) {
  return children;
}
