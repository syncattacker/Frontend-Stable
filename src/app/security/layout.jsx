export const metadata = {
  title: "Security",
  description:
    "How gopwnit secures accounts, sessions, and data — device-bound sessions, encrypted credentials, and a coordinated vulnerability disclosure process.",
};

export default function SecurityLayout({ children }) {
  return children;
}
