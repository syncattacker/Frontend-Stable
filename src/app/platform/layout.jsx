export const metadata = {
  title: "Platform",
  description:
    "What's on gopwnit — hacking labs, ten CTF challenge categories, structured learning paths, live leaderboards, and tools to host your own events.",
};

export default function PlatformLayout({ children }) {
  return children;
}
